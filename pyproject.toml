[project]
name = "yolo-det"
version = "1.0.0"
description = "YOLOv8n ONNX Inference Program"
requires-python = ">=3.10"
dependencies = [
    "ai-edge-litert>=2.1.0",
    "numpy>=1.24.0",
    "opencv-python",
    "zmq",
]

[project.scripts]
yolo-detect = "yolo_detect:main"
